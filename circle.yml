
machine:
  timezone: US/Eastern
dependencies:
  post:
    - bundle exec jekyll build
#add testing later on!
test:
  post:
   - echo 0
deployment:
  staging:
    branch: master
    heroku:
      appname: jordsite
