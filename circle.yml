
machine:
  timezone: US/Eastern
ruby:
  version: 2.3.1
dependencies:
  post:
    - bundle exec jekyll build
#add testing later on!
test:
  post:
    - bundle exec htmlproofer ./_site --check-html --disable-external

deployment:
  staging:
    branch: master
    heroku:
      appname: jordsite
