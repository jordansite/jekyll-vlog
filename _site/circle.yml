
machine:
  timezone: US/Eastern
dependencies:
  post:
    - bundle exec jekyll build
#add testing later on!
test:
  post:
   - echo test
deployment:
  staging:
    branch: master
    heroku:
      appname: jordsite
